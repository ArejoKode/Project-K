/**
 * 題目一：陣列過濾（6 分鐘）
 *
 * 請實作 filterPositive 函式：
 * - 接收一個數字陣列
 * - 回傳一個新陣列，只包含正數（大於 0 的數字）
 * - 不能使用內建的 filter 方法
 *
 * 範例：
 * filterPositive([1, -2, 3, -4, 5])  // 回傳 [1, 3, 5]
 * filterPositive([-1, -2])          // 回傳 []
 * filterPositive([0, 1, 2])         // 回傳 [1, 2]（0 不算正數）
 */

function filterPositive(arr) {
  // 請在這裡寫你的程式碼


}

// 測試用（可以執行看看結果對不對）
console.log(filterPositive([1, -2, 3, -4, 5]))  // 預期: [1, 3, 5]
console.log(filterPositive([-1, -2]))          // 預期: []
console.log(filterPositive([0, 1, 2]))         // 預期: [1, 2]
